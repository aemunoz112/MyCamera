package co.edu.anders.mycamera.ui.theme

import androidx.compose.ui.graphics.Color

// Colores personalizados para la app de cámara
val CameraPrimary = Color(0xFF2196F3)      // Azul vibrante
val CameraSecondary = Color(0xFF03A9F4)    // Azul claro
val CameraAccent = Color(0xFFFF5722)       // Naranja para acciones
val CameraBackground = Color(0xFF000000)    // Fondo negro para cámara
val CameraSurface = Color(0xFF1E1E1E)      // Superficie oscura

// Colores para tema claro
val LightPrimary = Color(0xFF1976D2)
val LightSecondary = Color(0xFF0288D1)
val LightAccent = Color(0xFFFF5722)

// Colores para tema oscuro
val DarkPrimary = Color(0xFF64B5F6)
val DarkSecondary = Color(0xFF4FC3F7)
val DarkAccent = Color(0xFFFF7043)